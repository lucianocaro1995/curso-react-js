//import "./Loading.css";

const Loading = () => {
    return (
        <div>Cargando</div>
    )
}

export default Loading

//Acá tengo que poner un spinner
//Este componente se utiliza poniendo el hook "const [loading, setLoading] = useState(true);" y luego llamando al setLoading donde queramos